_c75‍.x([["default",()=>_c75‍.o]]);const protectRoute = (redirectTo = "/") => (req, res, next) => {
    if(req.session.user) {
        return next();
    }

    return res.redirect(redirectTo)
}

_c75‍.d(protectRoute);